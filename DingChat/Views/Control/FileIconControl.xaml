<UserControl x:Class="cn.lds.chatcore.pcw.Views.Control.FileIconControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:cn.lds.chatcore.pcw.Views.Control"
             mc:Ignorable="d" Margin="0" Padding="0"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Name="FileIconUserControl"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="Grid" Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Width="32" Height="32" Source="{Binding Path=FileIcon}" />
        <TextBlock Grid.Row="1" Text="{Binding Path=FileName}" />
    </Grid>
</UserControl>
